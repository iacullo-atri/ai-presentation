Bootstrap a full modern looking "to-do" application that uses Django and django-rest-framework as the backend, and a frontend stored at  `/frontend` that uses vite, react, typescript, and tailwind for styling.  
  
Only provide minimal features: add new, check as done.